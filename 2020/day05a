#!/usr/bin/awk -f

function btod(i, d, p) {
	p = 1
	for (i = NF; i > 0; i--) {
		if ($i ~ /[BR]/)
			d += p
		p *= 2
	}
	return d
}

BEGIN { FS = "" }
      { if ((d = btod()) > max) max = d }
END   { print max }
